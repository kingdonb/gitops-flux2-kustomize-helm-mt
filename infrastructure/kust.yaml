apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: test-kust
  namespace: cluster-config
spec:
  interval: 1m
  path: "./infrastructure/nginx"
  prune: true
  timeout: 2m
  sourceRef:
    kind: GitRepository
    name: cluster-config